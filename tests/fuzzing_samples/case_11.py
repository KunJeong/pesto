from collections import deque
dq = deque([7, 3])
copied = deque(dq)
front = dq[0]
length = len(dq)
if dq:
    head = dq[0]
else:
    head = 0
items = list(dq)
item_count = len(items)
raise NotImplementedError('todo')
